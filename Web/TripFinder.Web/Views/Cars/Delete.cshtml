@model TripFinder.Web.ViewModels.Cars.CarDeleteViewModel

@{
    ViewData["Title"] = "Delete";
}

<section id="page-title-area" class="section-padding overlay">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8 form">
                <h3>Are you sure you want to delete your car from TripFinder?</h3>
                <form class="row mb-3" method="post">
                    <div class="col justify-content-center form-btn">
                        <button class="col-4" type="submit" asp-area="" asp-controller="Cars" asp-action="DeletePost" asp-route-id="@Model.Id"><i class="fa fa-trash"></i> Delete car</button>
                        <button class="col-4" type="submit" asp-area="" asp-controller="Cars" asp-action="Index"><i class="fa fa-undo"></i> Cancel</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</section>
